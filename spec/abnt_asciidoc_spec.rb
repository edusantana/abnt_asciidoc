require 'spec_helper'

describe AbntAsciidoc do
  it 'has a version number' do
    expect(AbntAsciidoc::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
